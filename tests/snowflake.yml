version: 2

models:
  - name: stage_orders
    columns:
      - name: order_status
        tests:
          - accepted_values:
              values: ['Completed','Pending', 'Cancelled']
  - name: stage_cutomers
    columns:
      - name: customer_id
        tests:
        - unique